<Window x:Class="RoomateManager.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RoomateManager"
        mc:Ignorable="d"
        Title="AddTaskWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="SegmentToggle" TargetType="ToggleButton">
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Margin" Value="0,6,6,0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="#ECEFF1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#27AE60"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#95A5A6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="Tên nhiệm vụ:" Width="120" VerticalAlignment="Center"/>
            <TextBox x:Name="txtTaskName" Width="360"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,6">
            <TextBlock Text="Mô tả:" Width="120" VerticalAlignment="Top"/>
            <TextBox x:Name="txtDescription" Width="360" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,6">
            <TextBlock Text="Tần suất:" Width="120" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbFrequency" Width="240">
                <ComboBoxItem IsSelected="True">Hàng ngày</ComboBoxItem>
                <ComboBoxItem>Hàng tuần</ComboBoxItem>
                <ComboBoxItem>Hàng tháng</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Width="100" Margin="6,0" Click="btnCancel_Click">Hủy</Button>
            <Button x:Name="btnOk" Width="100" Margin="6,0" Click="btnOk_Click" Background="#27AE60" Foreground="White">OK</Button>
        </StackPanel>
    </Grid>
</Window>
